// variable options:
// var - can be overwritten any time
// let can be overwritten any time use it as a local variable (should not be hoisted)
// const can be defined ones, must have initial value (array, and object props are modifiable)

const people = [
  {
    name: "John Doe",
    age: 28,
    sex: "male"
  },
  {
    name: "Jane Doe",
    age: 24,
    sex: "female"
  }
];

const numbers = [1, 2, 3, 4, 5, 6];

const strings = ["one", "two", "three", "four", "five", "six"];

console.log(numbers);
